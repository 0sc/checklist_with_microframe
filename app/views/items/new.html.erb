<h2 class="center"><%= @list.name %></h2>
<h4>New Item for</h4>
<div class="row">
  <%= render_partial 'form' %>
</div>

<div class="row">
  <%= link_to 'Back', "/lists/#{@list.id}/items" %>
</div>
